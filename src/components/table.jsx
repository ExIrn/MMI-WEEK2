

const MyTable = ({
classTableName
}) => {
    return(
        // <button onClick={buttonClick}>{buttonName}</button>
        <table className={classTableName}></table>
    )
}

export default MyTable